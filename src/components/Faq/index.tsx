import React from "react";
import { useTranslation } from "next-i18next";

const Faq = () => {
  const { t } = useTranslation("common");

  return (
    <div>
      asdsa
      <div>adssa</div>
    </div>
  );
};

export default Faq;
